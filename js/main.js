$('.menu').slicknav();

$('.slider-full').slick({
    slidesToShow: 4,
    slidesToScroll: 1,
    autoplay: true,
    autoplaySpeed: 2000,
  });

  $('.all_testimonial').slick({
    slidesToShow: 3,
    slidesToScroll: 1,
    autoplay: true,
    autoplaySpeed: 1400,
    dots: true,
   
  });
//  css3 animated headline codestart
$('.headline').animatedHeadline({
  
});
// css3 animated headline code end

// owl carousel plugin
$('.owl-carousel').owlCarousel();